let factory = function(i) {
    if (i == 0) {
        return 1;
    } else {
        return i * factory(i-1);
    }
}

println(factory(5))

let student = {
    "name": "zjc",
    "age": 20,
}

println(student["age"])
println(student["name"])

